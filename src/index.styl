cfu-gradient-1 = #4DD6D8
cfu-gradient-2 = #0097BF
text-color = #000000
light-text-color = rgba(0,0,0,0.6)

:root{
	--gradient-start: #000;
	--gradient-end: #000;
}

#scrollcolor
	position fixed
	top -100px
	left -100px
	bottom -100px
	right -100px
	background linear-gradient(to bottom right, cfu-gradient-1, cfu-gradient-2)
	background-size cover
	z-index: -999999;

html
	height 100%
body
	padding 0
	margin 0
	height 100%
	overflow-y: scroll;
#root
	display grid
	grid-template-columns 1fr 320px 1fr
	
ellipsis()
	text-wrap none
	white-space nowrap
	overflow hidden
	text-overflow ellipsis
italic()
	font-style italic
lightshadow()
	box-shadow 2px 2px 20px rgba(0,0,0,0.3);

.button
	display inline
	background linear-gradient(to bottom right, var(--gradient-start), var(--gradient-end))
	border-radius 10px
	padding-left 10px
	padding-right 10px
	padding-top 5px
	padding-bottom 5px
	height min-content
	color white
	text-decoration none
	font-family sans-serif
	font-weight bold
	lightshadow();
	& > .text
		padding 0

.content
	grid-column-start 2

@keyframes scalein
	from
		transform scaleY(0)
	to
		transform scaleY(1)

h1
	font-family sans-serif
	color white
	text-shadow 2px 2px 20px rgba(0,0,0,0.8)

.shortcut
	margin 10px
	background-color white
	padding 10px
	border-radius 10px
	box-shadow 2px 2px 20px rgba(0,0,0,0.4)
	overflow hidden
	font-family sans-serif
	position relative
	&.loading
		opacity 1
	& > .progressoverlay
		position absolute
		top 0
		left: 0
		right: 0
		bottom: 0
		border-radius 10px
		@supports (backdrop-filter: blur(3px))
			backdrop-filter blur(3px);
			background-color rgba(0, 151, 191, 0.2)
		@supports not (backdrop-filter: blur(3px))
			background-color rgba(0, 151, 191, 0.6)
		display grid
		grid-template-rows: 1fr max-content max-content 1fr
		& > .progress
			opacity 1
			background linear-gradient(to right, var(--gradient-start) 0%, var(--gradient-end) var(--progress), rgba(0,0,0,0) var(--progress))
			height 20px
			transition 100ms width, 0.1s opacity, 0.1s z-index
			margin 10px
			border-radius: 10px;
			&.loaded
				opacity 1
	& > .fullcontent
		padding-top 10px
		display none
		transform:scaleY(0);
		transform-origin: top center;
		transition 0.1s transform
		&.open
			animation: 0.2s scalein
			animation-fill-mode: forwards;
			display block
		& > .fulldescription
			white-space pre-wrap
			word-wrap: break-word;
	& > .short
		display grid
		grid-template-columns max-content 1fr max-content
		gap 10px
		& > .icon
			width: 40px;
			height: 40px;
			border-radius 10px
			lightshadow();
		& > .details
			display grid
			grid-template-rows 0 max-content 1fr
			max-height 40px;
			transition 0.1s grid-template-rows
			&.open
				grid-template-rows 1fr max-content 1fr
			& p
				ellipsis()
				padding 0
				margin 0
			& > .description
				ellipsis()
				italic()
				color light-text-color
				font-family serif
				transform:scaleY(1);
				transform-origin: bottom center;
				transition 0.1s transform
				&.open
					transform:scaleY(0);
		& > .buttons
			display grid
			max-height 40px;
			grid-template-rows 1fr min-content 1fr
	